import { CellData } from '../types/index';
import { CellUpdate } from '../helpers/CellUpdate';

export function FindPointingEntries(cells: Array<CellData>, updates: Array<CellUpdate>): boolean {
    return false;
}
